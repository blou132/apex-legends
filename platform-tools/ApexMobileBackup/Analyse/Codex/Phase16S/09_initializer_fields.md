# Initializer fields

Because no owner initializer was proven, no field write can be attributed to
construction without repeating the exhausted displacement-only scan.

`INITIALIZER_PLUS_0X45_VALUE = UNKNOWN`

`INITIALIZER_PLUS_0X1F0_VALUE = UNKNOWN`

`INITIALIZER_PLUS_0X1F8_VALUE = UNKNOWN`

The existing facts remain unchanged: `+0x1f0` is the confirmed persistent
Dolphin interface field, `+0x1f8` is probably related, and `+0x45` is persistent
first-extraction mode state.
