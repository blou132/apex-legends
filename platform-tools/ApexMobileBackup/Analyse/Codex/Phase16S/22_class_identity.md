# DolphinUpdater class identity

The semantic class remains probable from multiple prior named owner methods
and their consistent field behavior. Phase16S did not add the required
independent vtable-writer, constructor, or callback-backpointer convergence.

`DOLPHINUPDATER_CLASS_IDENTITY = PROBABLE`

The singleton Shutdown table cell cannot by itself upgrade identity to
confirmed.
