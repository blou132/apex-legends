# Alias code references

Because no alias or VTT address was recovered, no alias-address `ADR`,
`ADRP+ADD`, `ADRP+LDR`, literal, or relocation-backed reference can exist in
the authorized candidate set.

`ALIAS_CODE_REFERENCE_COUNT = 0`
