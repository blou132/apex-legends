# Data aliases

No relocation-backed alias points to the primary or secondary address point.
An independent aligned 64-bit scan of allocated non-executable data payloads
also found no exact raw value. Relocation and symbol metadata sections were
excluded from the raw-pointer control because their address-shaped fields are
not runtime pointer aliases.

- `PRIMARY_AP_ALIAS_COUNT = 0`
- `SECONDARY_AP_ALIAS_COUNT = 0`
- `GROUP_INTERNAL_ALIAS_COUNT = 0`
- Raw exact hits: `0 / 0`
