# Earliest observable divergence

Both historical axes reach GCloud/Puffer initialization. Phase15U then emits
the provider Init/version-manager witness; Phase16I does not. No retained
event exposes the earlier interface-acquisition state, `bUseIFSFile` result,
or first-extract mode before that point.

The earliest evidence difference is therefore Init execution, not a proven
pre-Init selector value.

```text
PHASE15U_PHASE16I_EARLIEST_DIVERGENCE = INIT_EXECUTION
```
