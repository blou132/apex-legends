# Remote dependence reassessment

Phase15U reached provider Dolphin Init and the version-manager path while its
Puffer/network path was already offline and failing. Phase16I also showed
offline Puffer failures but no Dolphin/version-manager witness. The shared
failure condition therefore does not discriminate the branch.

No retained successful remote response or server-derived value dominates the
Phase15U Init witness. Chronology alone is not used as causation.

```text
REMOTE_INPUT_BRANCH_CAUSATION = UNSUPPORTED
```
