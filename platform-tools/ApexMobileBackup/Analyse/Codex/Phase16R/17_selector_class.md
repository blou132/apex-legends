# Selector class

The retained evidence cannot reduce the run difference to one state class.
Possible upstream distinctions remain:

- whether the persistent interface had been acquired;
- `bUseIFSFile` and its first-extract preparation branch;
- other state in the opaque producer/selection region.

The `owner+0x45 == 0` class is removed as an absolute Init gate by the direct
rejoin to the shared dispatch. Its historical value can still affect Init
arguments, but not by itself establish whether Init was called.

```text
PHASE15U_PHASE16I_SELECTOR_CLASS = MULTIPLE_POSSIBLE
```
