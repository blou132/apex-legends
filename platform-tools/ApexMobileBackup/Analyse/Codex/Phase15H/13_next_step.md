# Next step

Phase15H resolves the cache-reuse question and moves the observed boundary past
the downloader into stable process-scoped graphics initialization and a
rendered Lightspeed splash/wait state. Do not repeat the downloader or CRC
work, and do not treat TDM/GCloud DNS failures as a proven game bootstrap gate.

The first unresolved transition is now from the confirmed graphics/splash
state to the first named Lua, `ClientLaunch`, Login, or server-list stage. The
run provides no evidence that any of those named stages executed.

A useful follow-up should stay offline and non-invasive, and should first
reanalyze already captured sanitized lifecycle/render timing or identify an
official application log source. It must not use memory maps, profiling,
debugger escalation, hooks, patches, network redirection, backend responses,
accounts, or phones.

```text
LAST_CONFIRMED_ANDROID_STAGE = GAMEACTIVITY_FOREGROUND_DRAWN_VISIBLE_AT_+300S
LAST_CONFIRMED_UE4_STAGE = APEX_GRAPHICS_BACKEND_INITIALIZATION_AND_RENDERED_SPLASH
LAST_CONFIRMED_CLIENT_STAGE = LIGHTSPEED_SPLASH_WITH_WAIT_UI
LAST_CONFIRMED_NETWORK_STAGE = TDM_ROUTE_RETRIES_FAILING_AT_NAME_RESOLUTION
FIRST_UNRESOLVED_TRANSITION = GRAPHICS_SPLASH_TO_NAMED_LUA_OR_LOGIN_STAGE
REPEAT_DOWNLOADER_OBSERVATION = NOT_REQUIRED
```
