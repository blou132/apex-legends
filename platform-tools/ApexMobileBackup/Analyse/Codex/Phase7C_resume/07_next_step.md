# Phase7C Resume - Next step

The original build inputs are now restored locally, but exact EventSystem terms are not raw-visible in any of the four PAK bodies. Repeating broader string scans is not justified.

The next legitimate evidence source is an authorized readable PAK manifest/index, file table, cache record, or virtual-file mapping from the same build. It must connect the probable logical module path to a concrete entry without decryption or protection bypass.

If such mapping is obtained:

1. prove the exact container and entry boundary;
2. classify only that entry as text, bytecode, compressed, encoded, encrypted, or unknown;
3. recover it only if directly readable without bypass;
4. locate the `0x138` subscriber and parser in local-only analysis;
5. publish only a cleaned summary, never proprietary Lua.

If reading the index or entry requires a key, decryption, exploit, or brute force, stop and document the limit.
