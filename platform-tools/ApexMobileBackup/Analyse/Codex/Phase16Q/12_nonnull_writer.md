# Non-null writer

No proven non-null assignment to `DolphinUpdater+0x1f0` was recovered. The
only exact write is the `Shutdown` clear, so there is no source register or
assigned-value provenance to slice.

```text
NON_NULL_WRITE_SITE = NONE_PROVEN
NON_NULL_WRITE_SOURCE_REGISTER = UNKNOWN
NON_NULL_WRITE_VALUE_SOURCE = UNKNOWN
```
