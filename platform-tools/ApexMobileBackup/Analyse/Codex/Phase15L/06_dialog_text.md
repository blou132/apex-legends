# Dialog text

The hierarchy resolves the complete user-visible dialog content.

Title:

```text
Unable to run on this device!
```

Message:

```text
App is packaged for OpenGL ES 3.1 but device has not met all the requirements for ES 3.1 :
	Device has OpenGL ES 3.1 support: NO
	Floating point render target support: NO
This device only supports OpenGL ES 2 but the app was not packaged with ES2 support.
```

Buttons: `OK`.

No token, account, device identifier, credential, or private query was present
in these nodes.
