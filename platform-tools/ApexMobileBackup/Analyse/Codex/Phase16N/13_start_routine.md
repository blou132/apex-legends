# Start routine

No dedicated target start routine is proven by the bounded static path. The
early event is a service call executed in caller context, not a worker-entry
witness.

- `START_ROUTINE_RESOLVED = NO`
- `START_ROUTINE_UNIQUE_TO_TARGET_ROLE = UNKNOWN`
