# Pre-protector window

The selected instance is visible by T0 + 3.302 s and has a useful GCloudCore
role event by T0 + 4.063 s. The first retained explicit ownership sample is
later, while the final watcher found ownership already present when the Puffer
correlation became actionable.

There is no `TracerPid` sample at either early event. Consequently, retained
evidence cannot prove that protector ownership began after them.

`PRE_PROTECTOR_WINDOW_EXISTS = UNKNOWN`
