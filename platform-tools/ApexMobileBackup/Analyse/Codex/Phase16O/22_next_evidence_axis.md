# Next evidence axis

The next useful question is no longer who consumes the result of the dead
static `CreateDolphin` reference. It is where the real Dolphin interface is
acquired on an executable path.

Any future phase must define a new bounded PC-only strategy. It must not become
a global `BLR` scan, broad libUE4 decompilation, runtime trace, or ptrace retry.

```text
NEXT_EVIDENCE_AXIS = PC_ONLY_IDENTIFY_REAL_DOLPHIN_INTERFACE_ACQUISITION_PATH
CURRENT_BLOCKER = REAL_DOLPHIN_INTERFACE_ACQUISITION_PATH_UNRESOLVED
```
