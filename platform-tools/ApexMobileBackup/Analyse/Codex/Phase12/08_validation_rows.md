# Validation rows

The requested multi-method validation is conditional on a candidate
GameActivity registration table. No such table was resolved, so zero rows were
validated.

| DEX method | DEX signature | Table name | Table signature | Native target | Result |
|---|---|---|---|---|---|
| `nativeResumeMainInit` | `()V` | `UNKNOWN` | `UNKNOWN` | `UNKNOWN` | not available |

```text
OTHER_ROWS_VALIDATED = 0
REGISTRATION_TABLE_CONFIDENCE = UNKNOWN
```

Inventing two or three rows from nearby strings would not validate a table and
was not attempted.
