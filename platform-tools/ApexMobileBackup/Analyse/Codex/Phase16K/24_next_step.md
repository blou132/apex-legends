# Next step

Do not run an active trace yet. The next bounded PC-only task should resolve
why the exact `libUE4.so` call sequence is shifted relative to its PLT ABI and
recover the real client dispatch representation, using executable mapping,
relocation ordering, exception/unwind metadata, and existing targeted Ghidra
state only.

If that static representation remains opaque, the next evidence request is an
owner-authorized comparison of already preserved states or a separately
authorized runtime observation with a deterministic local trigger. No live
service contact, response fabrication, or AppData modification is implied.

```text
NEXT_STEP = PC_ONLY_RESOLVE_LIBUE4_PLT_CALLSITE_REPRESENTATION_AND_CLIENT_DOLPHIN_DISPATCH
```
