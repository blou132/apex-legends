# AppData and remote dependence

No concrete read path links the unresolved selector to a package-private file,
SharedPreferences, SQLite, cache, persistent state, remote configuration, a
server response, or a Puffer/GCloud response.

Absence of such a path is not proof of independence; both classifications
remain unknown. No AppData or service was accessed to fill the gap.

```text
DOLPHIN_SELECTION_APPDATA_DEPENDENCE = UNKNOWN
DOLPHIN_SELECTION_REMOTE_DEPENDENCE = UNKNOWN
```
