# Selector semantics

No exact selector was recovered. In particular, the static evidence does not
support choosing update type, SDK mode, plugin availability, local/remote
configuration, platform, region/channel, first-run state, return code, or
object availability.

```text
DOLPHIN_SELECTION_PREDICATE_CLASS = UNKNOWN
DOLPHIN_SELECTION_PREDICATE_DESCRIPTION = Client Init dispatch and controlling value unresolved
```
