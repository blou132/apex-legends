# Next evidence axis

The independent UClass exists, but the authorized class-registration path
does not expose usable property metadata, a native function table, a setter,
or the `+0x38` owner producer.

`NEXT_EVIDENCE_AXIS = UNREAL_REFLECTION_OWNER_AXIS_EXHAUSTED`

Any future phase requires a genuinely new, bounded, non-opaque static anchor
that directly identifies either the `GameUpdateMgr+0x38` property/type or its
assignment producer. This phase does not authorize runtime tracing.
