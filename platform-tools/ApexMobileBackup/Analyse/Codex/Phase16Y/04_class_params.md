# Generated class parameters

No caller argument from `FUN_081b1348` structurally resolves to an
`FClassParams`, `FPropertyParams`, `PropPointers`, or class-scoped function
array. The recovered ABI passes class identity, layout, flags, callbacks,
superclass, and within-class accessors directly.

The native-registration and constructor callbacks are non-decodable/opaque.
They cannot be used as a substitute metadata source without crossing the
explicit protector boundary.

`GAMEUPDATEMGR_CLASS_PARAMS = NOT_RECOVERED_FROM_AUTHORIZED_ABI`

`GAMEUPDATEMGR_CLASS_PARAMS_CONFIDENCE = UNKNOWN`

The absence of a recognizable block in these direct arguments is an
observation about the audited path, not a global absence claim.
