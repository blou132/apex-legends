# Controls

## Property metadata control

No GameUpdateMgr property block was recovered, so a same-class neighboring
property control could not be run.

`UNREAL_PROPERTY_METADATA_CONTROL_VALID = NO`

## Class registration control

The adjacent compiled-in entries for `DolphinUpdater` and `GameUpdateMgr`
contain their exact accessors, matching package getter pattern, shared helper
ABI, lazy UClass cells, size/alignment fields, and callback positions.

`UNREAL_CLASS_REGISTRATION_CONTROL_VALID = YES`
