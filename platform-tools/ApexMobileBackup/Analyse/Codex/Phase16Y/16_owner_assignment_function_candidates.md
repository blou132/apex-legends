# Owner-assignment function candidates

No exact GameUpdateMgr native registration names were recoverable. Therefore
no function can be selected by an exact reflected name matching `Init*`,
`Initialize*`, `Setup*`, `Register*`, `Set*`, `Create*`, or `Start*`.

`GAMEUPDATEMGR_OWNER_ASSIGNMENT_FUNCTION_CANDIDATES = NONE_PROVEN`

`REFLECTED_CANDIDATE_PLUS0X38_ACCESS_COUNT = 0_PROVEN`

Code-shape-only candidates were deliberately excluded.
