# DolphinUpdater class literal

The full contiguous static string `/Script/PureClient.DolphinUpdater` is not
present in ASCII or UTF-16LE form. Unreal registration instead supplies the
package and class as separate arguments.

One unique UTF-16LE registration literal, `UDolphinUpdater`, is used with the
exact `/Script/PureClient` package. Its `DolphinUpdater` portion is materialized
by two equivalent class-accessor bodies that share one UClass cell.

- Exact full script-class literal found: `NO`
- Unique Unreal class-registration literal count: `1`
- Equivalent accessor materializations: `2`

Other raw `DolphinUpdater` occurrences were not promoted unless they belonged
to this exact class-registration context.
