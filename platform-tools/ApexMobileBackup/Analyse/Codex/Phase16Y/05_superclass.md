# Superclass

The exact superclass accessor supplied by `GameUpdateMgr` resolves to:

`/Script/Basic.ApexGameInstanceSubsystem`

Its accessor has the same validated class-registration shape and reports a
class size of `0x38`. The result is based on the exact package and class
literals reached through the `GameUpdateMgr` superclass argument.

`GAMEUPDATEMGR_SUPERCLASS_CONFIDENCE = CONFIRMED`
