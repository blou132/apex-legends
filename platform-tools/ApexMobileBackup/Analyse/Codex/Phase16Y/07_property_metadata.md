# GameUpdateMgr property metadata

No sanitized property table can be emitted because no class-scoped property
parameter block was recovered. Property name, kind, offset, size, flags, and
object-class type remain unavailable for every GameUpdateMgr field.

The phase did not perform a global reflection scan, generic UObject scan, or
global field-offset scan to compensate for this missing structure.

`GAMEUPDATEMGR_PROPERTY_METADATA = UNAVAILABLE_ON_AUTHORIZED_PATH`
