# New `+0x1f0` route

The callback argument mapping does not expose a new producer or lifecycle
successor for the primary interface field. It only proves that `+0x1f0` and
`+0x1f8` are both consumed by the same Init dispatch.

```text
NEW_PLUS1F0_PROVENANCE_ROUTE = NO
```
