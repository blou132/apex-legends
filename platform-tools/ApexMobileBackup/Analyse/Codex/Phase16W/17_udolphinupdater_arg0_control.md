# UDolphinUpdater argument-0 control

The other three validated CheckUpdate callers pass their own argument 0:

- `UDolphinUpdater::StartUpdate`
- `UDolphinUpdater::OnSrcUpdateFinished`
- `UDolphinUpdater::OnUpdateSuccess`

No recovered owner-assignment function is available to compare against those
arguments without reopening the exhausted caller axis.

```text
UDOLPHINUPDATER_ARG0_CONVERGENCE = UNKNOWN
```
