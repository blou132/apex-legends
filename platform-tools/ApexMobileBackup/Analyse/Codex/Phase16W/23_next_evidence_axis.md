# Next evidence axis

The callback register and field role are resolved, but construction and
non-null owner binding remain entirely before the readable CheckUpdate
continuation.

```text
NEXT_EVIDENCE_AXIS = CALLBACK_OWNER_ASSIGNMENT_OPAQUE_BOUNDARY
```

Any future work requires a new independent provenance anchor. Repeating global
field, constructor, vtable, generic indirect-call, or caller scans is not
justified.
