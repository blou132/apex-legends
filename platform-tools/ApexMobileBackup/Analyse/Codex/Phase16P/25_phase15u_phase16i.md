# Phase15U and Phase16I

Phase16P now locates the client-side Dolphin Init dispatch and two immediate
state gates. This narrows the historical difference: Phase15U passed the
pre-Init owner state, while Phase16I did not execute the branch.

The decisive values and the non-null acquisition occur in a protected or
statically opaque prelude, so the exact historical predicate remains unknown.

```text
PHASE15U_PHASE16I_BRANCH_DIFFERENCE_EXPLAINED = PARTIAL
```
