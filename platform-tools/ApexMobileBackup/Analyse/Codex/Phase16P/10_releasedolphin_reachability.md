# ReleaseDolphin reachability

There are no exact direct callsites to classify, so there is no reachable
exact `ReleaseDolphin` caller in the bounded direct-branch axis.

```text
RELEASEDOLPHIN_REACHABLE_CALLSITE_COUNT = 0
RELEASEDOLPHIN_REACHABILITY = NO_EXACT_DIRECT_CALLSITE
```
