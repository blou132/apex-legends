# Acquisition mechanism

No readable non-null writer reaches `DolphinUpdater+0x1f0`. A direct factory,
wrapper return, virtual factory, service lookup, injection, or transfer cannot
be selected without inventing evidence.

```text
REAL_DOLPHIN_ACQUISITION_MECHANISM = UNKNOWN
ACQUIRED_INTERFACE_IS_GCLOUDDOLPHIN = PROBABLE
```

Compatibility is probable because the persistent field is used by exact
DolphinUpdater lifecycle methods and its `+0x10` dispatch matches the known
`GCloudDolphinImp::Init` ABI. Assignment/vptr provenance is still missing.
