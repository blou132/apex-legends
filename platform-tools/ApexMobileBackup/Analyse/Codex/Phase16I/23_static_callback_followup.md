# Static callback follow-up

The bounded static follow-up required a concrete runtime slot `+0x28` target.
No target was captured, so no speculative module scan or unrelated Ghidra
analysis was performed.

```text
EXTERNAL_CALLBACK_CLASS = UNKNOWN
EXTERNAL_CALLBACK_SLOT_28_FUNCTION = UNKNOWN
LIBUE4_CALLBACK_ANCHOR = UNKNOWN
CALLBACK_NEXT_EDGE = RUNTIME_SLOT_28_TARGET_NOT_CAPTURED
```
