# Update manager edge

No concrete client callback function is available, so the bounded path does not
reach a named update, version, patch, resource-update, or Puffer manager in
`libUE4`.

No function is promoted based on naming or runtime timing.

```text
CLIENT_UPDATE_MANAGER = UNKNOWN
CLIENT_UPDATE_MANAGER_FUNCTION = UNKNOWN
UPDATE_STAGE_2_NEW_EVIDENCE = NO
```
