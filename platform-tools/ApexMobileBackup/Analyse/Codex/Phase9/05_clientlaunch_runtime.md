# ClientLaunch runtime

## Status

```text
CLIENTLAUNCH_OBSERVED = NO
```

No runtime chain could be captured. The Phase8 static witness remains unchanged:

- readable body string: `Client/Launch/ClientLaunch.lua`
- local-only container witness: `launch.pak`
- raw offset: `0x250517`
- classification: body witness, not a proven index entry or loader key

Phase9 does not promote this string to a logical module name, package-searcher result, final open path, provider selection, or chunk name.
