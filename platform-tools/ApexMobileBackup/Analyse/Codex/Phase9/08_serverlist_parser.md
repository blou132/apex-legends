# Server-list parser

## Status

```text
SERVERLIST_PARSER_FOUND = NO
```

No event handler became available, so Phase9 could not follow the response body into a parser or identify a response format, fields, destination, or writer for `GameServerBackupIpList`.

The following remain `UNKNOWN`:

- response serialization format
- fields read by Lua
- parser function or library
- storage destination
- runtime relationship to `Login + 0x150`

No JSON or other format is inferred without evidence.
