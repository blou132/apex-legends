# EventSystem runtime

## Status

```text
EVENTSYSTEM_OBSERVED = NO
```

No runtime lookup was available for `EventSystem`, `PostCppEvent`, or the Phase6 probable values:

- `Script/Tools/EventSystem/EventSystem.lua`
- `EventSystem.PostCppEvent`

The full module name, final extension, package-searcher candidate, effective provider, final open path, lookup result, chunk name, and chunk size remain `UNKNOWN`.

The Phase6 names remain `PROBABLE`; Phase9 provides no runtime confirmation and no correction.
