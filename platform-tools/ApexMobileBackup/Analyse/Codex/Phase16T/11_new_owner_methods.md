# New owner methods

The 94 bounded target functions were checked only for exact embedded
`DolphinUpdater::` identifiers. Of 71 available DolphinUpdater UTF-16
anchors, the only group match was the already-known `DolphinUpdater::Shutdown`
anchor inside ELF `0x04712c6c`.

`NEW_OWNER_METHOD_ANCHORS = NONE_BEYOND_KNOWN_SHUTDOWN`
