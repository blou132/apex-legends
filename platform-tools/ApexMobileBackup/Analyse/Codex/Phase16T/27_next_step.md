# Next step

Preserve the probable primary address point ELF `0x0ae56608`, confirmed
secondary address point ELF `0x0ae568b0`, and the exact `this - 0x28` thunk.

Do not perform broader scanning or runtime work under Phase16T. A future phase
must first justify a separate PC-only address-point writer provenance source,
then inspect only exact resulting references and bounded store windows.
