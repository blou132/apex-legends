# Allocation model

The evidence does not distinguish heap allocation, an embedded parent object,
a static singleton, or a service object.

`DOLPHINUPDATER_ALLOCATION_MODEL = UNKNOWN`

`CONSTRUCTION_ESTABLISHES_DOLPHIN_INTERFACE = UNKNOWN`
