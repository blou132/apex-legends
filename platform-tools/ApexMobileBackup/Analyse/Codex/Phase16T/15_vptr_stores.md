# Vptr stores

No exact code reference to either address point was recovered, so there was
no candidate instruction window in which a vptr store could be promoted.

`VIRTUAL_POINTER_STORE_COUNT = 0`

The store site, object base, object offset, and the expected secondary
`object+0x28` consistency check remain unknown.
