# Apex package state

The installed package is present and matches the preserved build identity.

```text
PACKAGE_PRESENT = YES
VERSION_NAME = 1.3.672.546
VERSION_CODE = 64003140
PRIMARY_CPU_ABI = arm64-v8a
DEBUGGABLE = NO
DATA_DIR_ACCESSIBLE_BY_SHELL = NO
RUN_AS_APEX_ALLOWED = NO_PACKAGE_NOT_DEBUGGABLE
```

The package was stopped before and after the audit. Phase15S issued no launch,
force-stop, install, uninstall, data-clear, or cache-clear command.
