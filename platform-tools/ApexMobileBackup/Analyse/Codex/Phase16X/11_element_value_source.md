# Element value source

The consumer proves that source element `+0x08` contains the selected
`GameUpdateMgr` UObject. No authorized producer path reaches the write which
populates that field.

```text
SELECTED_ELEMENT_VALUE_WRITE_SITE = UNKNOWN
SELECTED_ELEMENT_VALUE_SOURCE_REGISTER = UNKNOWN
SELECTED_ENTRY_VALUE_SOURCE = UNKNOWN
```
